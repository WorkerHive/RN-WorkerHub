import React from 'react';

import {
  Layout,
  Text
} from '@ui-kitten/components';

export default function DeviceList(props){
  return (
    <Layout style={{flex: 1, backgroundColor: '#dfdfdf', padding: 8}}>
      <Text category="h4">Devices</Text>
    </Layout>
  );
}
